query CartInput {
  shop {
    goals: metafield(namespace: "zuper_threshold", key: "goals") {
      value
      type
    }
  }
  cart {
    lines {
      id
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  discount {
    discountClasses
  }
}
